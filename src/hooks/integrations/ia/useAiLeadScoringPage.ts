/**
 * Adapter hook — Fase 2C IA / Lead Scoring
 * Reaproveita: useIntelligenceConfig (tabela lead_scoring_config / intelligence_config)
 * Não cria novo motor de scoring.
 */
export { useIntelligenceConfig as useAiLeadScoringPage } from "@/hooks/useIntelligenceConfig";
