# ============================================================
# .env.example — Variáveis obrigatórias para rodar o sistema
# independente de qualquer plataforma.
# ============================================================

# ── Supabase ──────────────────────────────────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# ── URL Pública do App ────────────────────────────────────────
# Usada para links, QR codes, OG tags, emails.
VITE_PUBLIC_URL=https://seu-dominio.com.br

# ── Supabase Secrets (configurar via Supabase Dashboard > Settings > Edge Functions) ──
# SUPABASE_URL=              (auto-populated by Supabase)
# SUPABASE_ANON_KEY=         (auto-populated by Supabase)
# SUPABASE_SERVICE_ROLE_KEY= (auto-populated by Supabase)
# SUPABASE_DB_URL=           (auto-populated by Supabase)

# ── Evolution API (WhatsApp) ──────────────────────────────────
# EVOLUTION_API_KEY=your-evolution-api-key

# ── Push Notifications (VAPID) ────────────────────────────────
# VAPID_PUBLIC_KEY=your-vapid-public-key
# VAPID_PRIVATE_KEY=your-vapid-private-key

# ── Segurança ─────────────────────────────────────────────────
# APP_URL_LOCKED=https://seu-dominio.com.br  (usado apenas para OAuth/callbacks — NUNCA editável via UI)
# APP_URL=https://seu-dominio.com.br
# ALLOWED_REDIRECT_HOSTS=seu-dominio.com.br
# CRON_SECRET=your-random-cron-secret

# ── Integrações por Tenant (configurar via Admin > Integrações) ──
# OpenAI API Key → armazenada em integration_configs por tenant
# SolarMarket Token → SOLARMARKET_TOKEN (secret global)
